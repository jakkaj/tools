---
schema_version: "1.0.0"
retro_id: "2026-05-19T03-13-43Z-claude-code-073f2025"
agent: claude-code
plan_id: "023-difficulty-ledger-skill"
started_at: "2026-05-19T01:30:00Z"
ended_at: "2026-05-19T03:13:43Z"
duration_ms: 6223000
summary: "FX001 Group A landed — 3 closure retros for review-driven compound + engineering-harness fixes."

entries:
  - id: RV-001
    kind: improvement-suggestion
    target: engineering-harness
    description: |
      Widened boot-time relevance filter in engineering-harness-v2 from
      `engineering-harness | tooling | infra` to also include
      `build | config | dependencies | env | auth | tests | observe`.
      Prior filter silently dropped 3 of 5 fixture target classes
      (build, config, project) — boot-time agents missed the most
      actionable friction. Confirmed bug via fixture grep before fix.
    references:
      - "fx-id:FX001"
      - "workshop-id:WS007"
      - "commit:4184305"
    system:
      compound:
        status: encoded
        resolved_by: "4184305"
        source: user
        first_seen_at: "2026-05-18T17:12:00Z"

  - id: RV-002
    kind: improvement-suggestion
    target: compound
    description: |
      Added `--json` output schema to compound-3-harvest/SKILL.md
      (stable JSON contract for retros + entries + top_clusters + harness).
      Added scripts/compound-value.sh — cross-CLI portable bash + jq
      pretty-printer that reads JSON on stdin and emits a compact
      6-line terminal view. Added `just compound-value` recipe and
      `Compound loop:` line in `just help` listing. Preserves workshop
      006 KISS rule (no on-disk index; view computed at read time).
    references:
      - "fx-id:FX001"
      - "workshop-id:WS007"
      - "commit:4184305"
    suggested_encoding: "scripts/compound-value.sh + just compound-value recipe"
    system:
      compound:
        status: encoded
        resolved_by: "4184305"
        source: user
        first_seen_at: "2026-05-18T17:12:00Z"

  - id: RV-003
    kind: improvement-suggestion
    target: compound
    description: |
      Added mandatory `## Validation` footer template to compound-2-bubble's
      `[e]ncode` action section. Every staged scratch/encode-<id>-<target>.diff
      must now end with Run / Expected / Compound lifecycle sub-sections.
      "Encoded" now means *the loop changed AND we can prove it*, not just
      *we wrote a patch*. Reviewers see verification path inline with the
      change.
    references:
      - "fx-id:FX001"
      - "workshop-id:WS007"
      - "commit:4184305"
    system:
      compound:
        status: encoded
        resolved_by: "4184305"
        source: user
        first_seen_at: "2026-05-18T17:12:00Z"

system:
  compound:
    bubble_action: "manual-write"
    notes: |
      Hand-authored per Option A from FX001's Compound Tracking section
      (canonical for fix workflows — plan-6a Step 8a only harvests
      difficulty/magic-wand/gift kinds, so improvement-suggestion entries
      don't flow through orchestrator retrospective). Demonstrates
      Compounding Test signals (a) action chosen, (b) status=encoded,
      (d) user did not disable. Signal (c) — subsequent session reads
      ledger — will be demonstrated by the next session in this repo
      via plan-1a Subagent 7 or engineering-harness-v2 § Known Difficulties.
---

# FX001 closure retro — Group A from external review

Three improvement-suggestion entries closing the loop on RV-001 (engineering-harness target taxonomy bug), RV-002 (compound-3-harvest `--json` + `just compound-value`), and RV-003 (compound-2-bubble validation footer template). All three encoded by commit `4184305` (`feat(compound, engineering-harness): FX001 Group A — RV-001/002/003`).

This retro is the **proof-by-existence** that the compounding loop closes on plan 023 itself — the system catches and resolves friction in its own design.

Next steps in the loop:
- **FX002** (Group B): RV-004 evidence pointers + schema v1.1.0 bump; RV-005 engineering-harness-v2 CREATE-mode UX
- **FX003 or plan 025** (Group C, dogfood-deferred): RV-006 cluster→regression-proof opt-in; RV-007 deterministic harvest logic to scripts
