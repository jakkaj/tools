# Harness Nucleus Consolidation — Implementation Plan

**Mode**: Simple
**Plan Version**: 1.0.0
**Created**: 2026-05-28
**Spec**: [harness-nucleus-spec.md](./harness-nucleus-spec.md)
**Status**: DRAFT

## Summary

Consolidate 6 compound+harness skills into 3 harness-themed loop-stage skills (`harness-1-boot`, `harness-2-observe`, `harness-3-retro`) under a new `skills/harness/` category, retire the philosophy skill by inlining its 5 principles, drop 2 setup-time skills to the user's separate engineering-harness setup effort, and cascade every cross-reference atomically so no SDD consumer, the minih cross-system contract, or the `engineering-harness.md` filename contract silently breaks. This is consolidation only (rename/merge/extract) — nothing new is added; the latent `plan-3-v3-architect` fallback-chain bug is repaired in the same commit train. Expected outcome: a coherent loop-stage skill family that is the runtime companion to an already-set-up engineering harness, with all old names gone (modulo immutable plan history).

## Target Domains

The repo has **no `docs/domains/registry.md`** — these boundaries are informal (proposed-but-unregistered). Formalizing them is a plan-025+ Workshop Opportunity, NOT in scope.

| Domain | Status | Relationship | Role |
|--------|--------|--------------|------|
| harness | proposed-but-unregistered | **create** + **modify** | New `skills/harness/` home for the 3 surviving loop-stage skills; retire the philosophy skill |
| compound | proposed-but-unregistered | **modify** | `skills/compound/` shrinks to `schemas/` only; 4 compound-* folders leave (2 renamed into harness/, 2 dropped) |
| sdd-pipeline | existing-implicit | **consume** | 8 `## Compound integration` appendices updated atomically; plan-3-v3-architect bug fixed; no SDD behavior change |
| dev-tooling | existing-implicit | **consume** | `justfile` recipe + `scripts/compound-value.sh` updated; `src/jk_tools/` mirror synced |

## Domain Manifest

| File | Domain | Classification | Rationale |
|------|--------|----------------|-----------|
| `skills/harness/harness-1-boot/SKILL.md` | harness | contract (new) | VALIDATE+STATUS extracted from engineering-harness-v2; CREATE dropped |
| `skills/harness/harness-2-observe/SKILL.md` | harness | contract (new) | Renamed from compound-1-track |
| `skills/harness/harness-3-retro/SKILL.md` | harness | contract (new) | Merge of compound-2-bubble (`--drain`) + compound-3-harvest (`--harvest`) |
| `skills/SDD/harness-is-the-product-v2/SKILL.md` | harness | delete | Retired; 5 principles distributed inline |
| `skills/SDD/engineering-harness-v2/SKILL.md` | harness | delete | Fully consumed (VALIDATE+STATUS moved, CREATE dropped) |
| `skills/compound/compound-0-setup/SKILL.md` | compound | delete | Dropped to separate setup effort |
| `skills/compound/compound-{1-track,2-bubble,3-harvest}/SKILL.md` | compound | delete (post-move) | Sources consumed by the 3 new harness skills |
| `skills/compound/schemas/**` | compound | unchanged (frozen) | minih cross-system contract — stays put (IC-01/IC-08) |
| `skills/SDD/{plan-1a-v2-explore,plan-2c-v2-workshop,plan-5-v2-phase-tasks-and-brief,plan-6-v2-implement-phase,plan-6-v2-implement-phase-companion,plan-6a-v2-update-progress,plan-7-v2-code-review,plan-8-v2-merge}/SKILL.md` | sdd-pipeline | cross-domain | 8 `## Compound integration` appendices → new names |
| `skills/SDD/plan-3-v3-architect/SKILL.md` | sdd-pipeline | cross-domain | CF-03 fallback-chain bug fix (lines 50, 231) — edited LAST |
| `CLAUDE.md`, `AGENTS.md`, `README_AGENTS.md`, `INSTALL.md`, `MIGRATION.md` | dev-tooling | cross-domain | 5 governance docs → new names + vocabulary-freeze |
| `justfile`, `scripts/compound-value.sh` | dev-tooling | cross-domain | `compound-value` recipe + `--json` consumer |
| `src/jk_tools/**` | dev-tooling | generated | Mirror — regenerated by `./scripts/sync-to-dist.sh` |
| `docs/plans/024-harness-nucleus/workshops/walkthroughs.md` | harness | doc (new) | Loop walkthrough + synthetic fixture (AC9) |
| `docs/plans/023-difficulty-ledger-skill/difficulty-ledger-skill-plan.md` | sdd-pipeline | doc (1-line) | Forward-pointer only (CF-06 immutable otherwise) |

## Key Findings

| # | Impact | Finding | Action |
|---|--------|---------|--------|
| 01 | Critical | 8 SDD skills carry `## Compound integration` appendices invoking compound by name (CF-01 / DC-08; the 8: plan-1a, plan-2c, plan-5, plan-6, plan-6-companion, plan-6a, plan-7, plan-8). NB `grep -l 'compound-[0-9]' skills/SDD/*/SKILL.md` returns **9** today — the 9th (`harness-is-the-product-v2`) is removed by T006-delete, NOT T009-rewrite, so AC1 only goes empty after T006 | T009 — atomic update; Done-When = grep gate (AC1, run AFTER T006), not hardcoded list |
| 02 | Critical | `scripts/compound-value.sh:28-33` parses **8** jq paths: `.harness.{maturity,verdict,boot_ms}` + `.entries.{total,open,encoded,suggested}` + `.top_clusters[]` (CF-02). `jq // 0` defaults mask a missing field | T011 — `harness-3-retro --harvest --json` preserves the full shape; verify field presence (AC4) |
| 03 | High | `plan-3-v3-architect/SKILL.md:50,231` carries a 2-deep `agent-harness.md`-first chain omitting canonical `engineering-harness.md` (CF-03). It is the architect skill (self-modification, PL-02) | T013 — fix to canonical-first 3-deep chain + Phase 0 creates `engineering-harness.md`; **edit LAST** (AC7) |
| 04 | High | Migrated bodies inherit dangling refs to DROPPED skills: `compound-1-track:58,121` → `compound-0-setup`; `engineering-harness-v2:248,268` → `--create` / `agent-harness-v2` (validation pass, FC BLOCKED) | T002/T004 — neutralize to no-op + `UNAVAILABLE` (no successor slug to re-point); AC13 gate on `skills/harness/` |
| 05 | High | `## Known Difficulties` ledger auto-seed (engineering-harness-v2 Step 4a) + `## Anti-Patterns` live inside the DROPPED CREATE mode — would be silently cut (validation pass) | **Resolved**: BOTH the *seed/write* AND any boot-time *read* are CREATE-time/setup concerns → out of scope (Non-Goal). Source-verified: neither VALIDATE nor STATUS reads `## Known Difficulties` today, so adding a boot-read would *add* behavior. `harness-1-boot` replicates VALIDATE+STATUS verbatim → nothing cut, nothing added. Re-introducing a boot-read is a deliberate future enhancement, not this consolidation (T004) |
| 06 | High | npx-skills does not auto-prune — old slugs linger in `~/.agents/skills/` after rename (PL-04 orphan trap) | T014 — explicit `rm -rf` of all 6 old slugs + `just doctor-skills` post-install (AC5/AC6) |
| 07 | Medium | Vocabulary fragility — 4 renames in 5 weeks; under-documented renames creep back (PL-05 / CF-05) | T010 — 5-doc atomic update + vocabulary-freeze paragraph (AC11) |
| 08 | Medium | Self-modifying SDD skills drive this plan's own execution (PL-02) | Sequence: T009 (appendices) then T013 (architect) come AFTER all harness/ + governance/tooling edits; T013 truly last |
| 09 | Low | Cascade enumeration drift — hardcoded file lists rot (PL-06) | Every cascade Done-When is a grep, not a list |

## Agent Harness Strategy

**Not applicable** (user decision, spec § Clarifications). This plan edits markdown skill bodies + scripts + governance docs and runs `git` / `npx` / `just` — there is no running application to Boot/Interact/Observe. Validation is via grep-audit gates + dogfood install + `just doctor-skills` (Lightweight testing, spec § Testing Strategy).

## Implementation

**Objective**: Land the 6→3 consolidation + the CF-03 bug fix as one atomic, grep-verified commit train, leaving no old names in live surfaces and no dangling references in the migrated bodies.

**Testing Approach**: Lightweight (spec § Testing Strategy). Grep-audit gates ARE the tests (Done-When columns). No mocks — real `.retro.md` fixtures only. Walkthroughs doc + synthetic fixture for the retained loop behavior. `just doctor-skills` + `npx` smoke install for deployment verification.

**Atomicity (PL-03)**: All source edits (T001–T013) land in a single commit. T012 (mirror sync) runs after every source edit. T014–T017 are post-source verification + docs. Within the session, SDD-skill edits (T009) and the architect bug fix (T013) come after the harness/, governance, and tooling edits; T013 is last (PL-02).

### Tasks

| Status | ID | Task | Domain | Path(s) | Done When | Notes |
|--------|-----|------|--------|---------|-----------|-------|
| [x] | T001 | Create the `skills/harness/` category directory | harness | `/Users/jordanknight/github/tools/skills/harness/` | Directory exists | Foundation |
| [x] | T002 | `git mv compound-1-track → harness-2-observe`; set frontmatter `name: harness-2-observe`; rewrite self-refs; **neutralize** the `compound-0-setup` self-heal suggestions (src lines 58,121) to "no-op + report UNAVAILABLE" | harness | `/Users/jordanknight/github/tools/skills/harness/harness-2-observe/SKILL.md` | File exists w/ `name: harness-2-observe`; `grep -nE 'compound-[0-9]\|compound-1-track' <file>` empty | KF04; R2; preserves silent-producer + magic-wand behavior 1:1 |
| [x] | T003 | Create `harness-3-retro` by MERGING compound-2-bubble (→ `--drain`) + compound-3-harvest (→ `--harvest`) into one SKILL.md; frontmatter `name: harness-3-retro`; preserve `[s/t/p/e/d/a]` menu + the full 8-path `--json` schema | harness | `/Users/jordanknight/github/tools/skills/harness/harness-3-retro/SKILL.md` | File exists; both modes documented; `--harvest --json` block lists `harness.{maturity,verdict,boot_ms}` + `entries.{total,open,encoded,suggested}` + `top_clusters[]`; `grep -nE 'compound-[0-9]' <file>` empty | KF02; AC4; merge confirmed lossless (validation) |
| [x] | T004 | Create `harness-1-boot` by extracting VALIDATE+STATUS bodies from engineering-harness-v2 **verbatim (add no behavior)**; DROP CREATE body; rewrite dangling `--create`/`agent-harness-v2` refs (src ~248,268) → setup-effort pointer or remove; full 3-deep **canonical-first** fallback chain | harness | `/Users/jordanknight/github/tools/skills/harness/harness-1-boot/SKILL.md` | File exists; VALIDATE+STATUS present, CREATE absent; does NOT add a `## Known Difficulties` read (stays a setup/future concern — KF05); `grep -nE '\-\-create\|agent-harness-v2' <file>` empty; reads `engineering-harness.md`→`agent-harness.md`→`harness.md`; reports `UNAVAILABLE` when all absent | KF04, KF05; AC8, AC13 |
| [x] | T005 | Distribute the 5 retired principles inline per spec § Phases Step 2 (P1→README+boot header; P2→observe; P3→retro; P4→boot; P5→README+each description) | harness | the 3 new SKILL.md + `/Users/jordanknight/github/tools/README.md` | `grep -l 'compounding value\|encode.*don.*document\|agents.*real.*users\|harness.*is.*the.*product' skills/harness/ README.md` returns the expected files | AC12; "encode don't document" applied to itself |
| [x] | T006 | Delete `skills/SDD/harness-is-the-product-v2/` | harness | `/Users/jordanknight/github/tools/skills/SDD/harness-is-the-product-v2/` | Directory absent | After T005 (principles already distributed) |
| [x] | T007 | Delete `skills/SDD/engineering-harness-v2/` (fully consumed) | harness | `/Users/jordanknight/github/tools/skills/SDD/engineering-harness-v2/` | Directory absent | After T004 |
| [x] | T008 | Delete `skills/compound/compound-0-setup/` + the 3 moved compound sources; confirm `## Known Difficulties` seed + `## Anti-Patterns` are NOT relocated into this repo (setup-effort concern) | compound | `/Users/jordanknight/github/tools/skills/compound/` | `skills/compound/` contains only `schemas/` | KF05; after T002/T003 git mv |
| [x] | T009 | Update the 8 SDD `## Compound integration` appendices → invoke `harness-2-observe` / `harness-3-retro --drain` / `harness-3-retro --harvest`; remove `/compound-0-setup`, `/engineering-harness-v2 --create`, `/agent-harness-v2` suggestions (point at setup effort or drop) | sdd-pipeline | the 8 SKILL.md (see Domain Manifest) | `grep -l 'compound-[0-9]' skills/SDD/*/SKILL.md` empty; `grep -l 'harness-2-observe\|harness-3-retro' skills/SDD/*/SKILL.md` = the 8 files; `grep -rlE '\-\-create\|agent-harness-v2\|compound-0-setup' skills/SDD/*/SKILL.md` empty (excl. plan-3-v3 until T013) | AC1 (run after T006); KF01, KF08; PL-02 (late). **Refs to clear are NOT only in the appendix** — also `plan-1a:244,246` (Workshop block, `agent-harness-v2 --create`) + `plan-6a:164` (`compound-0-setup`). Trust the grep Done-When, not the section heading |
| [x] | T010 | Update 5 governance docs → new names, catalog rows, install snippets (fix the `--skill harness-is-the-product-v2` 404s in README_AGENTS); add CLAUDE.md vocabulary-freeze paragraph | dev-tooling | `CLAUDE.md`, `AGENTS.md`, `README_AGENTS.md`, `INSTALL.md`, `MIGRATION.md` (repo root) | AC2 + AC3 grep gates empty for old names; each doc names `harness-N-*`; AC11 freeze paragraph present | KF07; PL-05 |
| [x] | T011 | Update tooling: `justfile` `compound-value` recipe + `scripts/compound-value.sh` to consume `harness-3-retro --harvest --json` (rename-only if shape preserved; else atomic rewrite of script+recipe+help) | dev-tooling | `/Users/jordanknight/github/tools/justfile`, `/Users/jordanknight/github/tools/scripts/compound-value.sh` | `harness-3-retro --harvest --json \| just compound-value` (or renamed) produces non-empty output; all 8 jq paths present in the JSON. NB the script reads harvest **JSON on stdin**, NOT a `.retro.md` file; recipe/script embed no skill name, so this is likely help-text-only rename | AC4; KF02; R3 |
| [x] | T012 | Run `./scripts/sync-to-dist.sh` AFTER all source edits | dev-tooling | `/Users/jordanknight/github/tools/src/jk_tools/` | Sync exits clean; mirror reflects new names; `git diff --stat src/jk_tools/` shows expected renames | Must run after T001–T011 |
| [x] | T013 | **(LAST)** Fix `plan-3-v3-architect/SKILL.md` (~lines 49 + 230, content-anchored) → the harness-doc fallback-chain READS use canonical-first 3-deep (`engineering-harness.md`→`agent-harness.md`→`harness.md`); Phase-0 create instruction writes `engineering-harness.md`. **Do NOT blindly rewrite every `agent-harness.md`** — only the governance-doc fallback reads + the Phase-0 create line; the agent-harness vs engineering-harness conceptual distinction elsewhere is intentional | sdd-pipeline | `/Users/jordanknight/github/tools/skills/SDD/plan-3-v3-architect/SKILL.md` | fallback reads canonical-first; Phase-0 writes `engineering-harness.md` | AC7; KF03; PL-02 — edit LAST |
| [x] | T014 | Cleanup + dogfood: `rm -rf ~/.agents/skills/{compound-0-setup,compound-1-track,compound-2-bubble,compound-3-harvest,engineering-harness-v2,harness-is-the-product-v2}/`; `just install-skills-from-source`; `just doctor-skills`; `npx skills add jakkaj/tools -a claude-code -g` | dev-tooling | `~/.agents/skills/`, `~/.claude/skills/` | `just doctor-skills` no orphans; 6 old slugs absent; 3 new slugs present | AC5, AC6; KF06; PL-04 |
| [x] | T015 | Run the full grep-audit gate suite (AC1, AC2, AC3, AC13) and confirm all pass | (verify) | repo root | AC1/AC2/AC3/AC13 all return their expected (empty / N-file) results | Final integrity gate. **Expected residue**: stale `compound-2-bubble`/`compound-3-harvest` strings inside frozen `skills/compound/schemas/` (system.compound.schema.json + fixtures) survive intentionally (frozen contract, AC2-excluded) — NOT a cascade miss; G3's repo-wide grep is unreachable for this reason, which is why AC13's `skills/harness/` scope is the real gate |
| [x] | T016 | Create `walkthroughs.md` tracing `harness-2-observe` → `harness-3-retro --drain` → `harness-3-retro --harvest` against a synthetic `.retro.md` fixture | harness | `/Users/jordanknight/github/tools/docs/plans/024-harness-nucleus/workshops/walkthroughs.md` | File traces the 3-skill loop end-to-end with a real fixture | AC9 |
| [x] | T017 | Add a 1-line forward-pointer atop `difficulty-ledger-skill-plan.md` noting the rename to plan-024 names | sdd-pipeline | `/Users/jordanknight/github/tools/docs/plans/023-difficulty-ledger-skill/difficulty-ledger-skill-plan.md` | Forward-pointer line present at top | AC10; CF-06 (otherwise immutable) |

### Acceptance Criteria

- [ ] AC1 (run after T006): `grep -l 'compound-[0-9]' skills/SDD/*/SKILL.md` empty; `harness-2-observe\|harness-3-retro` hits the 8 appendix files (T009)
- [ ] AC2: old names gone from `skills/` + 5 governance docs (incl. legacy `agent-harness-v2`); immutable history excluded (T010, T015)
- [ ] AC3: 5 governance docs reference new names; `compound-[0-9]`-anchored grep empty (T010)
- [ ] AC4: `harness-3-retro --harvest --json` preserves the full 8-path schema; `just compound-value` succeeds (T003, T011)
- [ ] AC5: `just doctor-skills` clean; 6 old `~/.agents/skills/` slugs removed (T014)
- [ ] AC6: `npx skills add jakkaj/tools` installs 3 new slugs; 6 old gone (T014)
- [ ] AC7: plan-3-v3-architect canonical-first 3-deep chain + Phase 0 creates `engineering-harness.md` (T013)
- [ ] AC8: `harness-1-boot` reads the 3-deep chain + reports `UNAVAILABLE` gracefully (T004)
- [ ] AC9: `walkthroughs.md` traces the loop with a synthetic fixture (T016)
- [ ] AC10: plan-023 forward-pointer present (T017)
- [ ] AC11: vocabulary-freeze recorded — commit message + CLAUDE.md paragraph (T010)
- [ ] AC12: 5 principles present inline at planned locations (T005)
- [ ] AC13: `grep -rlnE 'compound-[0-9]\|engineering-harness-v2\|agent-harness-v2\|--create' skills/harness/*/SKILL.md` empty (T002, T004)

### Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| R1: Vocabulary regression — old names creep back (PL-05) | Medium | High | AC2/AC3/AC13 grep gates as Done-When on T015 |
| R2: npx orphan trap — old slugs linger (PL-04) | High | Medium | T014 explicit `rm -rf` + `just doctor-skills` |
| R3: Tooling silent-breakage — `--json` shape regresses, masked by `jq // 0` (CF-02) | Medium | High | AC4 verifies field *presence* across all 8 paths, not just exit code (T011) |
| R4: Dangling refs survive in migrated bodies (validation FC-BLOCKED) | Medium | High | T002/T004 neutralize to `UNAVAILABLE`; AC13 greps the new `skills/harness/` location |
| R5: Known Difficulties / Anti-Patterns silently cut with CREATE (validation HIGH) | Low | High | KF05 resolution: seed AND boot-read both out of scope (setup/future); boot replicates VALIDATE+STATUS verbatim → nothing cut AND nothing added (T004) |
| R6: Self-modifying SDD skill edited mid-run (PL-02) | Low | Medium | T009/T013 sequenced last; T013 (architect) truly last; atomic commit |
| R7: Mirror desync — `src/jk_tools/` not regenerated | Low | Medium | T012 runs `sync-to-dist.sh` after all source edits |

---

*Plan status: VALIDATED WITH FIXES — Simple Mode, 17 tasks, single phase. Ready for `/plan-6-v2-implement-phase --plan "<this file>"`.*

---

## Validation Record (2026-05-28)

### Validation Thesis

**Raison d'être**: Turn the validated spec into an atomic, grep-verified consolidation that `/plan-6-v2-implement-phase` can execute task-by-task, preserving every concept (nothing cut), adding nothing, and breaking no SDD consumer / minih contract / filename contract.

**Proof target**: Implementation (plan-6 builds from it). **Actual**: Implementation-ready — line-anchored tasks, real grep gates, PL-02 sequencing concrete.

**Thesis source**: `harness-nucleus-spec.md` + `research-dossier.md` + the prior spec-validation 5 HIGHs.

**Thesis verdict**: Advanced — lossless consolidation-only; all dossier findings + 5 prior HIGHs mapped to tasks/gates; extraction track correctly fenced.

**Main thesis risk (fixed this pass)**: KF05 originally framed a boot-time `## Known Difficulties` read as "preserved," but neither VALIDATE nor STATUS reads it in source — it was a net-new addition. Removed from T004 so "nothing added" holds strictly.

| Agent | Lenses | Issues | Verdict |
|-------|--------|--------|---------|
| Coherence + Completeness | Coherence, Completeness, Proof-Level Fit | 2 MED, 2 LOW | YES-with-fixes (all 13 ACs + 8 spec groups mapped; ordering sound) |
| Thesis + Risk | Thesis Alignment, Risk, Hidden Assumptions | 1 MED (KF05), 1 LOW | Advanced — nothing-added now honest |
| Grep-gates + Forward-Compat | Forward-Compatibility, Integration & Ripple, Deployment & Ops | 1 MED, 2 LOW | 5/5 consumers PASS (gates verified against live repo) |

### Forward-Compatibility Matrix

| Consumer | Requirement | Failure Mode | Verdict | Evidence |
|----------|-------------|--------------|---------|----------|
| plan-6 implementer | Execute T001–T017 from Done-Whens + paths | Title-scoped edit misses non-appendix refs | PASS | Grep Done-Whens authoritative; T009 note added |
| 8 SDD appendices | `compound-[0-9]` → `harness-N-*` atomically | Stale slug breaks auto-fire | PASS | 9 live hits → 8 rewritten (T009) + 1 deleted (T006); AC1 after T006 |
| compound-value.sh + justfile | All 8 jq paths preserved | `jq // 0` masks dropped field | PASS | 8 paths confirmed L28-33; AC4 verifies presence; script is producer-name-agnostic |
| minih frozen contracts | `skills/compound/schemas/**` untouched | Schema drift | PASS | 3 schemas + 5 fixtures present; no task touches them; T008 preserves `schemas/` only |
| plan-3-v3-architect self-edit | T013 last; not the executor | Mid-run self-mutation | PASS | plan-6 does not delegate to plan-3-v3 (verified); T013 last; KF03 bug confirmed |

**Thesis alignment**: Value claim advanced; target Implementation, actual Implementation-ready; main risk (KF05 net-new read) fixed this pass.

**Outcome alignment**: Outcome = "name the loop stage each skill serves and retire one skill, WITHOUT breaking the 8 SDD consumers, the minih cross-system contract, or the engineering-harness.md filename contract." — The plan as written (post-fixes) advances this: loop-stage renames, the retirement, all 8 SDD appendices via T009 grep, minih `schemas/**` frozen and untouched, and the `engineering-harness.md` canonical chain fixed by T013 all hold PASS.

**Standalone?**: No — downstream consumer `/plan-6-v2-implement-phase` + the 8 appendices + tooling + minih.

Overall: **VALIDATED WITH FIXES** — no CRITICAL/HIGH; the one thesis-honesty issue (KF05 net-new read) + grep-gate documentation nits fixed in this pass. Ready for `/plan-6-v2-implement-phase`.
