# Original ask — unified-planning-doc
**Captured**: 2026-06-16T05:20:35Z  ·  **By**: /the-flow

> i do thing its too large, i do want them in one step.
>
> we will run the flow and go form there for this build... simple mode, but full flow

**Context (not part of the verbatim ask):** originated from a review of the external
refactor brief at `scratch/paste/20260616T040625.md`, which proposed consolidating the
`specify` (1b) and `architect` (3) stages of `the-flow` into one canonical planning
document. The user accepted the direction with one decision pinned: **artifact shape =
one document** (`<slug>-plan.md`, business spec on top, implementation plan below).
Build to be done on-pattern (flow-architecture: flow-blind sub-skills, deterministic
routing, `just check-flow` green; preserve the workshop/backpressure seam but only when
it is live; no over-built migration — historical plans stay as-is).
